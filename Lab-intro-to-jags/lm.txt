
model {  
   u ~ dnorm(0, 0.01); 
   C ~ dnorm(0,0.01);
   tau.r ~ dgamma(0.001,0.001); 
   sd.r <- 1/sqrt(tau.r); 
   
   for(i in 1:N) {
      X[i] <- u + C*c[i];
      EY[i] <- X[i]
      Y[i] ~ dnorm(EY[i], tau.r);
   }
}  
