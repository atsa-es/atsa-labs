
model {  
   u ~ dnorm(0, 0.01); 
   tau.q ~ dgamma(0.001,0.001); 
   sd.q <- 1/sqrt(tau.q); 
   phi ~ dnorm(0, 1); 
   
   EX[1] <- X[1];
   for(i in 2:N) {
      EX[i] <- u + phi * X[i-1]; 
      X[i] ~ dnorm(EX[i], tau.q);
   }
}  
